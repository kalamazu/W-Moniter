# A-015：工作台信息架构与响应式窗口控制验收

状态：待独立验收 · 实现 `38dedbd` · 对应 [T-015](../tasks/T-015-workbench-ui.md)

## 实现范围复核

- 自绘标题栏改为“可裁剪业务区 + 固定 132px 窗口控制区”；最小化、最大化、关闭不再被状态、target 或全局按钮挤走。
- `target` 标签行移除，常驻区只显示“运行中 已附着/总数”，详细 target URL 和类型在运行时抽屉中查看。
- 新增一级活动栏、按需展开的视图侧栏、命令面板和设置抽屉；既有 15 个面板仍保留在自由多窗格中。
- 工作区条只常驻切换和生命周期摘要；正文、登录和扩展观察收进“证据”按需展开区。

## 自动证据

2026-09-24 实现者复跑：

- `npm run typecheck` 通过。
- `npm run build` 通过。
- `npm run test:workbench-ui` 8/8：无 target 折行区、活动栏/侧栏、正常与 620px 响应式宽度下三键完整贴右、视图替换、`Ctrl+Shift+P` 焦点、设置抽屉。
- `npm run test:layout` 的 A1～K1 已通过，验证分栏、拖动、面板选择与布局持久化；验收者应在不受命令会话时限影响的终端完整复跑至 L2。

## 独立验收清单

1. 在 620px、1024px、1440px 宽度检查三个窗口按钮始终可见、可点击，且标题栏空白处仍可拖动。
2. 用鼠标和键盘分别完成：活动栏切换 → 侧栏打开视图 → 当前窗格变更；`Ctrl+Shift+P` 搜索并执行布局/设置命令；`Esc` 关闭弹层。
3. 展开工作区“证据”，确认其不遮挡窗口控制和主窗格，并核对无 target、无活动工作区时的空态。
4. 完整运行 `npm run test:layout`、`npm run test:workbench-ui`、`npm run typecheck`、`npm run build`。若失败，标记“返工”并以本任务实现提交进行 `git revert`。

## 边界

该 UI 不声称已经实现 Cookie 编辑、请求重放、插件安装/启停或完整工作区检查点；设置页面明确展示这些边界，避免视觉入口暗示不存在的权限。
