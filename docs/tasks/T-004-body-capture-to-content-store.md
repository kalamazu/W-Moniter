# T-004：响应正文写入 ContentStore

状态：可认领
蓝图映射：M1 §4
优先级：P0
依赖：T-003

## 目标

把 CDP 响应正文采集迁到 ContentStore 引用；请求列表只保留预览和元数据，正文读取走引用。取消“因正文大于阈值直接不采集”的静默路径。

## 验收

- 超过旧 `BODY_MAX_BYTES` 的响应能产生完整 ContentRef；UI/API 预览仍有限制。
- 采集失败写入明确 capture-gap，不能伪装为无正文。
- 旧 body 读取接口在兼容期可读取新引用。
- A-004 覆盖大正文、二进制和失败路径。
