### D11 实时 = 「查库 + 自增 id 游标」，不另起一套推送

事件流要「尽量实时」，第一反应是让采集侧直接往订阅者推。**不做**，理由是同一条数据走两条路
迟早不一致：推的那份要自己维护缓冲、自己处理慢消费者和断线重连，一旦和库里对不上，
排查时根本不知道以谁为准。

结论：**只有库是真相**。`/events` 与 `/ws` 的 `since` 是**自增 id**（不是时间戳 —— 时钟会回拨，
id 不会），返回里给 `nextSince`，调用方拿回来反复拉即可，语义是「只拿新增，不重复吐」。
UI 面板也照这个来（700–900ms 一次增量轮询）。唯一一个「像推送」的东西是 HTTP 侧的
`GET /events/stream`（SSE）：它内部也不过是**按 `interval` 查库**再把增量写出去，
所以它和轮询读的是同一份数据，不会各说各话。MCP 侧连这个门面都不给 —— agent 会循环调工具。

反悔了会怎样：真要做推送，先得回答「谁背压、断了怎么续、和库不一致时以谁为准」，
否则就是把一个可验证的问题换成一个不可验证的问题。
